# Heroes3_templates
Heroes 3 Hota templates that are fair for 3 human players and support large maps

# Copy from gdrive:

```bash
sudo mount -t drvfs G: /mnt/g
rsync -av --delete /mnt/g/Mój\ dysk/Heroes3_templates/ gdrive_templates/
```

# Tempaltes
